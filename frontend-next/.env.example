# Set EXACTLY ONE of the following:

# Option A — proxy via Next.js rewrites (server-only, no CORS needed)
BACKEND_URL=http://localhost:5000

# Option B — client calls backend directly (mirrors old VITE_BACKEND_URL; backend must allow CORS for your frontend origin)
# NEXT_PUBLIC_BACKEND_URL=https://your-backend.example.com
